
  <div class="div2">
  {{> results}}
  </div>
  <div class="dani">{{results.length}}results</div>

  <ul>
    {{#each results as |result|}}
    <div><a  class="headers" href="{{result.url}}">{{result.title}}</a></div>
  <div><p class="div">{{result.description}}</p></div>
  <div class="parapading"><li><a href="{{result.url}}">{{result.url}}</a></li></div>
  {{/each}}
  </ul>
